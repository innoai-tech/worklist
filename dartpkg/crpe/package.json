{
  "name": "@innoai-tech/dart-crpe",
  "version": "0.1.0",
  "description": "container registry pocket edition",
  "license": "MIT",
  "type": "module",
  "dependencies": {
    "@octohelm/dart-context": "^0.1.2",
    "@octohelm/dart-logr": "^0.2.0",
    "@octohelm/dart-roundtripper": "^0.2.0",
    "@innoai-tech/dart-storage": "workspace:^"
  },
  "scripts": {
    "fmt": "dart fix --apply && dart format -o write ./lib",
    "test": "dart test",
    "gen": "dart run build_runner build --delete-conflicting-outputs",
    "postinstall": "bunx @octohelm/dartpkg postinstall"
  },
  "exports": {
    "bun": "./pubspec.yaml"
  }
}